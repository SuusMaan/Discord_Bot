name: Discord Notifications

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, reopened, closed]

jobs:
  discord-notification:
    runs-on: ubuntu-latest
    steps:
      - name: Send Discord Notification (Commit)
        if: github.event_name == 'push'
        run: |
          curl -H "Content-Type: application/json" -X POST \
          -d '{
            "embeds": [
              {
                "title": "🚀 Новый коммит в ${{ github.repository }}",
                "description": "**Сообщение коммита:**\n${{ github.event.head_commit.message }}\n\n**Автор:** ${{ github.actor }}",
                "color": 5814783,
                "fields": [
                  {
                    "name": "🌍 Ветка",
                    "value": "`${{ github.ref }}`",
                    "inline": true
                  },
                  {
                    "name": "🔗 Ссылка на коммит",
                    "value": "[Перейти к коммиту](${{ github.event.head_commit.url }})",
                    "inline": true
                  }
                ],
                "footer": {
                  "text": "GitHub Actions • ${{ github.event.head_commit.timestamp }}"
                }
              }
            ]
          }' ${{ secrets.DISCORD_WEBHOOK_URL }}

      - name: Send Discord PR Notification
        if: github.event_name == 'pull_request'
        run: |
          curl -H "Content-Type: application/json" -X POST \
          -d '{
            "embeds": [
              {
                "title": "🔀 Pull Request: ${{ github.event.pull